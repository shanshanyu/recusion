#include <stdio.h>
int fib(n){
 if(n==0) return 0;
 if(n==1) return 1;
 else {
   return fib(n-1)+fib(n-2);
 }
}

//
if  ....  return 
else .... return   求阶乘   一次调用，这两个语句只会执行一个

if ... return 
if .... return 
else ... return   fib     一次调用，这三个语句只会执行一个

if  ... return 
else if ... return
else ... return    一次调用，这三个语句只会执行一个  
